规则|解释|严重级别| 启用状态
----|----|-----|-----------
enforce variables to be declared either together or separately in functions|翻译|次要|false
EsLint issues that are not yet known to the plugin|翻译|主要|false
limit the number of angular components per file (y001)|翻译|严重|false
require $on and $watch deregistration callbacks to be saved in a variable|翻译|主要|false
require a consistent DI syntax|翻译|主要|false
require a consistent order of module dependencies|翻译|主要|false
require a newline after each call in a method chain|翻译|次要|false
require all DI parameters to be located in their own line|翻译|主要|false
require an empty line before return statements|翻译|次要|false
require and specify a capture variable for this in controllers (y032)|翻译|主要|false
require and specify a consistent component name pattern (y120, y121)|翻译|次要|false
require and specify a consistent function style for components ('named' or 'anonymous') (y024)|翻译|主要|false
require and specify a prefix for all component names|翻译|次要|false
require and specify a prefix for all constant names (y125)|翻译|次要|false
require and specify a prefix for all controller names (y123, y124)|翻译|次要|false
require and specify a prefix for all directive names (y073, y126)|翻译|次要|false
require and specify a prefix for all factory names (y125)|翻译|次要|false
require and specify a prefix for all filter names|翻译|次要|false
require and specify a prefix for all module names (y127)|翻译|次要|false
require and specify a prefix for all provider names (y125)|翻译|次要|false
require and specify a prefix for all service names (y125)|翻译|次要|false
require and specify a prefix for all value names (y125)|翻译|次要|false
require and specify consistent use $scope.digest() or $scope.apply()|翻译|主要|false
require arrow functions as callbacks|翻译|次要|false
require braces around arrow function bodies|翻译|次要|false
require calls to isNaN() when checking for NaN|翻译|主要|false
require const declarations for variables that are never reassigned after declared|翻译|严重|false
require constructor names to begin with a capital letter|翻译|主要|false
require default cases in switch statements|翻译|主要|false
require destructuring from arrays and/or objects|翻译|次要|false
require DI parameters to be sorted alphabetically|翻译|主要|false
require empty lines around comments|翻译|提示|false
require error handling in callbacks|翻译|严重|false
require for-in loops to include an if statement|翻译|主要|false
require function names to match the name of the variable or property to which they are assigned|翻译|提示|false
require generator functions to contain yield|翻译|次要|false
require identifiers to match a specified regular expression|翻译|次要|false
require JSDoc comments|翻译|次要|false
require let or const instead of var|翻译|主要|false
require object keys to be sorted|翻译|次要|false
require or disallow  Yoda  conditions|翻译|次要|false
require or disallow an empty line after variable declarations|翻译|提示|false
require or disallow assignment operator shorthand where possible|翻译|次要|false
require or disallow initialization in variable declarations|翻译|次要|false
require or disallow method and property shorthand syntax for object literals|翻译|次要|false
require or disallow named function expressions|翻译|次要|false
require or disallow newline at the end of files|翻译|主要|false
require or disallow newlines around directives|翻译|次要|false
require or disallow newlines around variable declarations|翻译|次要|false
require or disallow padding within blocks|翻译|提示|false
require or disallow semicolons instead of ASI|翻译|提示|false
require or disallow spacing around embedded expressions of template strings|翻译|提示|false
require or disallow spacing around the * in yield* expressions|翻译|次要|false
require or disallow spacing between function identifiers and their invocations|翻译|提示|false
require or disallow strict mode directives|翻译|次要|false
require or disallow trailing commas|翻译|提示|false
require or disallow Unicode byte order mark (BOM)|翻译|次要|false
require parentheses around arrow function arguments|翻译|次要|false
require parentheses around immediate function invocations|翻译|次要|false
require parentheses when invoking a constructor with no arguments|翻译|主要|false
require parenthesis around regex literals|翻译|次要|false
require quotes around object literal property names|翻译|次要|false
require require() calls to be placed at top-level module scope|翻译|严重|false
require rest parameters instead of arguments|翻译|次要|false
require return statements after callbacks|翻译|主要|false
require return statements to either always or never specify values|翻译|主要|false
require spacing around infix operators|翻译|提示|false
require spread operators instead of .apply()|翻译|次要|false
require super() calls in constructors|翻译|次要|false
require symbol descriptions|翻译|次要|false
require template literals instead of string concatenation|翻译|主要|false
require the use of === and !==|翻译|主要|false
require the use of controllerAs in routes or states (y031)|翻译|主要|false
require to use angular.mock methods directly|翻译|主要|false
require using Error objects as Promise rejection reasons|翻译|主要|false
require var declarations be placed at the top of their containing scope|翻译|主要|false
require variables within the same declaration block to be sorted|翻译|次要|false
unittest inject functions should only consist of assignments from injected values to describe block variables|翻译|主要|false
use $cookies instead of $cookieStore|翻译|严重|false
use $document instead of document (y180)|翻译|主要|false
use $interval instead of setInterval (y181)|翻译|主要|false
use $q(function(resolve, reject){}) instead of $q.deferred|翻译|主要|false
use $timeout instead of setTimeout (y181)|翻译|主要|false
use $window instead of window (y180)|翻译|主要|false
use angular.element instead of $ or jQuery|翻译|主要|false
use angular.forEach instead of native Array.prototype.forEach|翻译|主要|false
use angular.isArray instead of typeof comparisons|翻译|主要|false
use angular.isDate instead of typeof comparisons|翻译|主要|false
use angular.isDefined and angular.isUndefined instead of other undefined checks|翻译|主要|false
use angular.isFunction instead of typeof comparisons|翻译|主要|false
use angular.isNumber instead of typeof comparisons|翻译|主要|false
use angular.isObject instead of typeof comparisons|翻译|主要|false
use angular.isString instead of typeof comparisons|翻译|主要|false
use factory() instead of service() (y040)|翻译|主要|false
use the $log service instead of the console methods|翻译|主要|false
Use the new api to build components|翻译|主要|false